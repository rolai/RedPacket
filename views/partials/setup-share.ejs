<script type="text/javascript">
wx.ready(function () {
    wx.onMenuShareAppMessage({
        title: '<%= share.title %>', // 分享标题
        desc: '<%= share.desc %>', // 分享描述
        link: '<%= share.link %>', // 分享链接
        imgUrl: '<%= share.imgUrl %>', // 分享图标
        success: function () {
            // 用户确认分享后执行的回调函数

        },
        cancel: function () {
            // 用户取消分享后执行的回调函数
        }
    });

    wx.onMenuShareTimeline({
        title: '<%= share.timelineTitle || share.title %>', // 分享标题
        link: '<%= share.link %>', // 分享链接
        imgUrl: '<%= share.imgUrl %>', // 分享图标
        success: function () {
            // 用户确认分享后执行的回调函数
        },
        cancel: function () {
            // 用户取消分享后执行的回调函数
        }
    });

});

</script>
